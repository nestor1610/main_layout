import { ContentScrollDirective } from './content-scroll.directive';

describe('ContenScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new ContentScrollDirective();
    expect(directive).toBeTruthy();
  });
});
